---
interface Props {
  navItems: {
    text: string;
    href: string;
  }[];
}

const currentYear = new Date().getFullYear();
---

<footer class="bg-dark text-white mt-16">
  <div class="mx-auto px-4 max-w-7xl pt-12 pb-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
      <div>
        <h3 class="text-xl font-bold mb-4 text-white">Astro WP</h3>
        <p class="text-gray-300 mb-4">
          Integração entre WordPress e Astro usando GraphQL e Tailwind CSS.
        </p>
      </div>
    </div>

    <div
      class="border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center">
      <p class="text-gray-400">
        &copy; {currentYear} Astro WP. Todos os direitos reservados.
      </p>
    </div>
  </div>
</footer>
